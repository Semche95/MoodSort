/**
 * Constants for card and UI styling
 */

// ---------------------------------------------------------------------------
// Card opacity states (drag vs static)
// ---------------------------------------------------------------------------

/** Opacity of a card while it is being dragged */
export const DRAGGING_OPACITY: number = 0.5
/** Default (resting) opacity for cards */
export const DEFAULT_OPACITY: number = 1

// ---------------------------------------------------------------------------
// Card scaling
// ---------------------------------------------------------------------------

/** Screen width (in px) at which cards render at native size */
export const CARD_REFERENCE_WIDTH: number = 2560

// ---------------------------------------------------------------------------
// Stack highlight
// ---------------------------------------------------------------------------

/** Padding (in px) added around the stack bounding box highlight */
export const STACK_HIGHLIGHT_PADDING: number = 20

/** Height (in px) of the stack drag handle bar */
export const STACK_HANDLE_HEIGHT: number = 22
